# ws_a usb_cam

This is a minimal package named `usb_cam` for the ROS workspace overlay demonstration.

It is intentionally small: the task only needs to show that workspace A contains a package named `usb_cam`.
